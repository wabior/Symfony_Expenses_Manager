<!DOCTYPE html>
<html>
<head>
    {% block head %}
        <meta charset="UTF-8">
        <meta name="csrf-token" content="{{ csrf_token('update-status') }}">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}My Website{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
            {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>

<body class="min-h-screen">
<div class="min-h-screen flex flex-col">
    <header>
        {% block menu %}
            {% include 'menu.html.twig' with {'menu_items': menu_items} %}
        {% endblock %}
    </header>  

    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="container mx-auto alert alert-{{ type }} bg-{{ type == 'success' ? 'green' : 'red' }}-100 border border-{{ type == 'success' ? 'green' : 'red' }}-400 text-{{ type == 'success' ? 'green' : 'red' }}-700 px-4 py-3 rounded mb-4">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %} 

    <main class="flex-grow">
        {% block content %}
        
        {% endblock %}
    </main>

    <footer class="bg-gradient-to-r from-gray-800 to-gray-900 text-white py-8 mt-auto">
        <div class="container mx-auto px-4">
            <div class="grid md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-xl font-bold mb-4 flex items-center">
                        <svg class="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M4 4a2 2 0 00-2 2v4a2 2 0 002 2V6h10a2 2 0 00-2-2H4zm2 6a2 2 0 012-2h8a2 2 0 012 2v4a2 2 0 01-2 2H8a2 2 0 01-2-2v-4zm6 4a2 2 0 100-4 2 2 0 000 4z"/>
                        </svg>
                        ExpenseManager
                    </h3>
                    <p class="text-gray-300">Zarządzaj swoimi wydatkami w nowoczesny i efektywny sposób.</p>
                </div>
                <div>
                    <h4 class="text-lg font-semibold mb-4">Szybkie linki</h4>
                    <ul class="space-y-2">
                        <li><a href="{{ path('home') }}" class="text-gray-300 hover:text-white transition-colors">Strona główna</a></li>
                        <li><a href="{{ path('expenses') }}" class="text-gray-300 hover:text-white transition-colors">Wydatki</a></li>
                        <li><a href="{{ path('categories') }}" class="text-gray-300 hover:text-white transition-colors">Kategorie</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-lg font-semibold mb-4">Kontakt</h4>
                    <p class="text-gray-300">Masz pytania? Skontaktuj się z nami.</p>
                    <p class="text-gray-300 mt-2">© 2024 Expense Manager. Wszelkie prawa zastrzeżone.</p>
                </div>
            </div>
        </div>
    </footer>

</div>
{% block javascripts %}
    <script src="{{ asset('build/runtime.js') }}"></script>
    <script src="{{ asset('build/app.js') }}"></script>
{% endblock %}
</body>
</html>
